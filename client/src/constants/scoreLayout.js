/** Stave width in pixels (per measure). */
export const MEASURE_WIDTH = 300
/** Vertical space for one system (stave + padding). */
export const SYSTEM_HEIGHT = 140
/** Left indent for the first measure on each system. */
export const FIRST_MEASURE_INDENT = 40
/** Max measures per system before wrapping. */
export const MEASURES_PER_SYSTEM = 4
/** Top offset of the first system. */
export const SYSTEM_TOP_OFFSET = 20
/** Vertical center of a stave within a system. */
export const STAVE_CENTER_Y = SYSTEM_TOP_OFFSET + 60
